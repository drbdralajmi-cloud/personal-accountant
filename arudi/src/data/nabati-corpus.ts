/**
 * مكتبة الشعر النبطي.
 *
 * ═══ لماذا تبدأ فارغة ═══
 *
 * طُلب أن تضمّ المكتبة آلاف الأبيات مصنّفةً بالوزن والقالب والقافية
 * والموضوع والشاعر والدولة والمدرسة. والبنية جاهزةٌ لذلك كلّه، والمحرّك
 * يصنّف الوزن والقالب والقافية بنفسه من نصّ البيت.
 *
 * وأما الأبيات نفسها ونسبتُها إلى شعرائها فلا تُولَّد: نسبةُ بيتٍ إلى
 * شاعرٍ خبرٌ عن إنسانٍ حقيقيّ، ونسبتُه إلى دولةٍ أو مدرسةٍ حكمٌ يُنقل عن
 * مصدر. فلو مُلئت هذه المكتبة بأبياتٍ مولَّدة ونُسبت إلى شعراء لصارت
 * مرجعاً يُفسد ما بُني ليحفظه. ولذلك تبدأ فارغةً وتُملأ من ثلاثة أبواب:
 *
 *   ١) لوحة التحكّم (/admin) — إدخالٌ يدويّ بيتاً بيتاً.
 *   ٢) هذا الملفّ — لمن يريد إضافةً دفعةً واحدة في المستودع.
 *   ٣) واجهة الاستيراد — ملفّ JSON بالبنية نفسها.
 *
 * وفي كل الأبواب يمرّ البيت على المحرّك قبل أن يُقبل: يُقاس وزنه، فإن لم
 * يستقم على طَرقٍ معلوم رُفض أو وُسم «يحتاج مراجعة». فالمكتبة لا تقبل ما
 * لم يتحقّق منه المحرّك.
 */

export type School = 'نجدية' | 'حجازية' | 'شمالية' | 'جنوبية' | 'خليجية' | 'بدوية' | 'حضرية';

export interface CorpusEntry {
  id: string;
  sadr: string;
  ajz?: string;
  /** الشاعر — لا يُترك فارغاً، ولا يُكتب إلا بنسبةٍ متحقَّق منها. */
  poet: string;
  country?: string;
  school?: School;
  topic?: string;
  /** المصدر الذي نُقل عنه: ديوان، أو تسجيل، أو رواية. */
  source?: string;
  /** سنة القول أو النشر إن عُرفت. */
  year?: number;
  /**
   * الطَّرق كما ينسبه المصدر. ويبقى حكم المحرّك مستقلّاً عنه، فإن اختلفا
   * عُرض الاثنان ولم يُرجَّح أحدهما بغير حجّة.
   */
  claimedTariq?: string;
}

/**
 * الأبيات الموثّقة. تبدأ فارغةً — انظر شرح الملفّ أعلاه.
 *
 * مثال البنية (مشروحٌ لا مُدرَج):
 *   {
 *     id: 'unique-id',
 *     sadr: 'صدر البيت',
 *     ajz: 'عجز البيت',
 *     poet: 'اسم الشاعر',
 *     country: 'السعودية',
 *     school: 'نجدية',
 *     topic: 'الغزل',
 *     source: 'ديوان كذا، ص ٠٠',
 *     claimedTariq: 'المسحوب',
 *   }
 */
export const NABATI_CORPUS: CorpusEntry[] = [];

export const TOPICS = [
  'الغزل',
  'المدح',
  'الحكمة',
  'الوصف',
  'الفخر',
  'الرثاء',
  'العتاب',
  'الحنين',
  'المحاورة',
  'الوطن',
] as const;

export const SCHOOLS: School[] = [
  'نجدية',
  'حجازية',
  'شمالية',
  'جنوبية',
  'خليجية',
  'بدوية',
  'حضرية',
];
